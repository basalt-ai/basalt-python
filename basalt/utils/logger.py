"""Legacy logger placeholder kept for backwards compatibility imports."""

raise ImportError(
    "basalt.utils.logger was removed. The SDK no longer exposes a custom logger"
    " type; use Python's logging module instead."
)
